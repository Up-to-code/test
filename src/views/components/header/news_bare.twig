<div class="news-bare">
  {% for news in component.news_navbare %}
  <div
    class="h-6 bg-black flex justify-center items-center hover:bg-opacity-90 transition-all duration-300 ease-in-out"
  >
    <span class="text-xs font-bold px-5 py-2 text-white">{{ news.title }}</span>
  </div>
  {% endfor %}
</div>
