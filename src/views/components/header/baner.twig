<div>
    <salla-slider type="banner">
        {% for banner in component.baner %}
        <div class="min-h-[100px] bg-cover bg-center" style="background-image: url('{{ banner.image}}');">

        </div>

        {% endfor %}
    </salla-slider>
</div>